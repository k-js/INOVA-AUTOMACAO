# Dependências da análise de imagem das capas (src/visao.py).
#
# Ficam separadas do requirements.txt porque pesam ~1 GB e levam 2-3 min para
# instalar. Só o workflow das capas as instala; a publicação diária não carrega
# esse peso.
#
# A versão CPU do torch é a que o runner usa — sem GPU, a GPU seria 700 MB
# baixados à toa.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
transformers
Pillow
opencv-python-headless
