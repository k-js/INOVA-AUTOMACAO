# Dependências da análise de imagem das capas (src/visao.py).
#
# Ficam separadas do requirements.txt porque pesam ~1 GB e levam 2-3 min para
# instalar. Só o workflow das capas as instala; a publicação diária não carrega
# esse peso.
#
# Os DOIS indices sao necessarios, nesta ordem:
#
#   --index-url        o do PyTorch vem primeiro para o torch resolver na
#                      versao +cpu. So com --extra-index-url o pip ainda podia
#                      trazer a build com CUDA do PyPI, ~800 MB inuteis num
#                      runner sem GPU.
#   --extra-index-url  o PyPI de volta, senao transformers e Pillow nao sao
#                      encontrados: o indice do PyTorch so hospeda os pacotes
#                      dele.
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple
torch
transformers
Pillow
